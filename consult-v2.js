(()=>{const thread=document.querySelector('#consultThread'),user=document.querySelector('#userMessage'),answer=document.querySelector('#assistantMessage'),again=document.querySelector('#newConsult'),send=document.querySelector('#sendConsult'),input=document.querySelector('#consultInput');function statedBudget(text){const m=text.replace(/,/g,'').match(/(\d+(?:\.\d+)?)\s*(万円|千円|円)/);if(!m)return null;const n=Number(m[1]);return Math.round(n*(m[2]==='万円'?10000:m[2]==='千円'?1000:1));}function intent(text){if(/上がりかけ|上昇|勢い|モメンタム|伸びそう/.test(text))return'momentum';if(/テンバガー|爆発|化け|何倍/.test(text))return'multibagger';if(/安定|配当|守り|大型/.test(text))return'stable';if(/何個|何種類|分散|一株ずつ/.test(text))return'split';return'open';}function reply(text){const monthly=Number(localStorage.getItem('dfl-monthly-budget'))||5000,budget=statedBudget(text)||monthly,type=intent(text),yen=budget.toLocaleString('ja-JP');const lead=type==='momentum'?`${yen}円以内で「上がりかけ」を探す。安いだけの株ではなく、直近で値動きが強まり始めた銘柄を優先したい。`:type==='multibagger'?`${yen}円以内で、大きく伸びる余地を優先して探す。値動きは荒くても、事業の伸びしろが大きい銘柄が対象。`:type==='stable'?`${yen}円以内で、安定性を優先して探す。配当・利益の安定・財務を重く見る。`:type==='split'?`${yen}円を複数銘柄に分ける前提で考える。1株価格と組み合わせの相性を先に見る。`:`${yen}円以内で、相談内容に合う買い方を考える。`;return `${lead}\n\nDream20には限定せず、市場全体から候補を探す。次は候補を3社までに絞り、「なぜ今か」「1株いくらか」「予算内でどう買うか」を並べる。`;}send.onclick=()=>{const text=input.value.trim();if(!text){input.focus();return}localStorage.setItem('dfl-last-consult',text);user.textContent=text.length>42?text.slice(0,42)+'…':text;thread.hidden=false;input.closest('.consult-input-wrap').hidden=true;answer.innerHTML='<div class="thinking"><span></span><span></span><span></span></div>';setTimeout(()=>{answer.textContent=reply(text);again.hidden=false},450)};again.hidden=true;again.onclick=()=>{thread.hidden=true;input.closest('.consult-input-wrap').hidden=false;input.value='';input.focus()};})();